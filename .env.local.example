# Supabase
# Vite は `VITE_` で始まる環境変数のみブラウザ側に渡します
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Dev auto login (optional)
# 開発時はデフォルトで自動ログインを試みます（テストユーザー1）。
# 無効化したい場合は 0 を指定してください。
VITE_DEV_AUTO_LOGIN=
# ログアウト済みフラグがあっても自動ログインを強制したい場合は 1
VITE_DEV_AUTO_LOGIN_FORCE=
VITE_DEV_AUTO_LOGIN_EMAIL=test1@example.com
VITE_DEV_AUTO_LOGIN_PASSWORD=testpassword1
